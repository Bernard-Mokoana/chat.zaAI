{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKER",
    "dockerfilePath": "backend/server/Dockerfile",
    "context": "."
  },
  "deploy": {
    "startCommand": "uvicorn main:api --host 0.0.0.0 --port 3500",
    "numReplicas": 1
  }
}
